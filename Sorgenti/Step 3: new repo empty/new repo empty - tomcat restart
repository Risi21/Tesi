TODO Batch in automatico

crea un database mysql dinamico, es n1

copia il .war dalla directory a tomcat7/webapps (es kiamalo n1.war)

aspetta che la deploya

modifica n1/WEB-INF/web.xml , il percorso dei 2 bootstrap.properties diventa
/srv/n1/bootstrap.properties

crea cartella /srv/n1

modifica proprietario cartella settandolo a tomcat:
sudo chown -R tomcat7:tomcat7 /srv/n1

copia file bootstrap.properties e repository.xml dentro a /srv/n1

modifica bootstrap.properties, inserisci nei 2 percorsi /srv/n1

modifica repository.xml
cambia utente mysql, password, nome database

modifica web.xml in tomcat webapps per dirgli dov è il bootstrap.properties

riavvia tomcat
sudo service tomcat7 restart

dentro a /srv/n1 vengono create in automatico le cartelle repository e workspace,
viene riempito il database n1 con ttt le tabelle

disabilita utente anonymous per il repository

cambia password admin

crea / rimuovi utenti
...
